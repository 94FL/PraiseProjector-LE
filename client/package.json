{
  "name": "pp-client",
  "version": "2.0.1",
  "private": true,
  "description": "PraiseProjector browser client",
  "scripts": {
    "build": "npm run build:release",
    "build:debug": "esbuild ./pp-api.ts --bundle --format=iife --global-name=P --platform=browser --target=es2018 --sourcemap --outfile=../public/app/pp-api.js",
    "build:release": "esbuild ./pp-api.ts --bundle --format=iife --global-name=P --platform=browser --target=es2018 --minify --outfile=../public/app/pp-api.js",
    "format": "prettier --write \"**/*.{ts,tsx,js,json,md,yml,yaml}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,json,md,yml,yaml}\""
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.25.10",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "color-calendar": "^1.0.7",
    "core-js": "^3.19.1",
    "fp-ts": "^2.16.11",
    "io-ts": "^2.2.22",
    "yalps": "^0.5.7"
  }
}
